/* global it describe */

const assert = require('assert');
const request = require('request');

describe('my app test', function myAppTest() {
  it('gets all the messages', function doesSomethingTest() {
    const messages = { 0: 'hey', 1: 'my message', 2: 'message 3' };
    assert.equal(response.length, Object.keys(messages).length);
  });
});
