/* global it describe */

const assert = require('assert');

describe('my app test', function myAppTest() {
  it('does something', function doesSomethingTest() {
    assert.ok(true, 'Did something');
  });
});
